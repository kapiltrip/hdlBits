# 069 — 100-bit binary adder

| Field | Value |
|---|---|
| Day | Day 03 |
| Last successful submission | 2026-06-25 5:26:53 PM |
| Course location | Circuits → Combinational Logic → Arithmetic Circuits |
| HDLBits identifier | `adder100` |
| Attempts | 2 total: 1 incorrect, 0 compile error, 0 simulation error |
| Success rate | 50% |
| Source | [Open original HDLBits problem](https://hdlbits.01xz.net/wiki/adder100) |
| Files | [Screenshot](../../images/Day%2003/069-adder100.png) · [Verilog solution](../../solutions/Day%2003/069-adder100.sv) |

## Problem and saved submission

![100-bit binary adder problem and saved submission](../../images/Day%2003/069-adder100.png)

## Saved Verilog solution

```verilog
module top_module( 
    input [99:0] a, b,
    input cin,
    output cout,
    output [99:0] sum 
);
genvar i ; 
    wire [100:0] carry ;
    assign carry[0]=cin;
    generate
        for(i=0;i<100;i++)begin : calling
            fa fa1(
                .a(a[i]),
                .b(b[i]),
                .cin(carry[i]),
                .cout(carry[i+1]),
                .sum(sum[i])
            );
        end
    endgenerate
    assign cout = carry [100];
endmodule
module fa(
    input wire a,b,cin,
    output wire cout,sum
);
    assign sum = a^b^cin ;
    assign cout = a&b | b&cin| a&cin;
endmodule
```

## What I learned

_Fill this in during revision._
