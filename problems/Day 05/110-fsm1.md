# 110 — Simple FSM 1 (asynchronous reset)

| Field | Value |
|---|---|
| Day | Day 05 |
| Last successful submission | 2026-06-27 1:04:37 AM |
| Course location | Circuits → Sequential Logic → Finite State Machines |
| HDLBits identifier | `fsm1` |
| Attempts | 9 total: 2 incorrect, 5 compile error, 1 simulation error |
| Success rate | 11% |
| Source | [Open original HDLBits problem](https://hdlbits.01xz.net/wiki/fsm1) |
| Files | [Screenshot](../../images/Day%2005/110-fsm1.png) · [Verilog solution](../../solutions/Day%2005/110-fsm1.sv) |

## Problem and saved submission

![Simple FSM 1 (asynchronous reset) problem and saved submission](../../images/Day%2005/110-fsm1.png)

## Saved Verilog solution

```verilog
module top_module(
    input clk,
    input areset,    // Asynchronous reset to state B
    input in,
    output out
);//  

    parameter A=0, B=1;
    reg state, next_state;

    always @(*) begin    // This is a combinational always block
        next_state=state;
        case (state)
            A:next_state=(in)?A:B;
            B:next_state=(in)?B:A;
        endcase
    end

    always @(posedge clk, posedge areset) begin    // This is a sequential always block
        // State flip-flops with asynchronous reset
        if(areset)
            state<=A;
        else 
            state <=next_state;
    end

    // Output logic
    // assign out = (state == ...);
    assign out = (state == A);    
endmodule
```

## What I learned

_Fill this in during revision._
